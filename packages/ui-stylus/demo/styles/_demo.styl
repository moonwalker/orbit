html {
  height: 100%;
}
body {
  height: 100%;
  min-height: 100%;
}

.demo-header {
  fixed(left top right)
  z-index: 1000;
  background: $dark;

  &__inner {
    margin: 0 auto;
    padding: 0 $space-medium;
    max-width: $container-max-width;
  }

  &__title {
    padding: $space-xsmall $space-xsmall $space-xsmall 0;
    font-weight: bold;
    type-scale(medium)
    text-decoration: none;
  }

  &__link {
    inline-block();
    relative();
    padding: $space-xsmall;
    color: $light;
    font-weight: bold;
    text-decoration: none;

    &:hover {
      color: rgba($light, 0.8);
    }

    &.active::after {
      block(100%, 2px);
      absolute(left bottom);
      background: $light;
    }
  }
}

.demo-content {
  box(cover);
  debug-baseline(rgba($gray, 0.2));
}

.demo-title {
  margin: 0 0 $space-large;
  border: 0px solid $gray-dark;
  border-bottom-width: 2px;
  padding-bottom: s('calc(%s - %s)', $space-medium, @border-bottom-width);
}

.demo-container {
  position: relative;
  z-index: 2;
  width: 100%;
  max-width: $container-max-width;
  margin: 0 auto;
  padding: ($space-xxxlarge + $space-medium) $space-medium $space-medium;
}

.demo-examples {
  margin: 0 0 $space-small;
  flex();
  align-items: flex-start;

  > * {
    margin-right: $space-xsmall
  }
}
