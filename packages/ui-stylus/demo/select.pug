extends _layout

block content
  .demo-examples(style = "max-width: 280px")
    select.ui-select(
      placeholder="Select option"
    )
      option Option A
      option Option B
      option Option C
      option Option D

  h2 Validation states

  .demo-examples(style = "max-width: 280px")
    select.ui-select.ui-select--valid(
      placeholder="Select option"
    )
      option Option A
      option(selected) Option B
      option Option C
      option Option D

  .demo-examples(style = "max-width: 280px")
    select.ui-select.ui-select--invalid(
      placeholder="Select option"
    )
      option Option A
      option(selected) Option B
      option Option C
      option Option D

  h3 Size variations

  .demo-examples(style = "max-width: 280px")
    select.ui-select.ui-select--small(
      placeholder="Select option"
    )
      option Option A
      option Option B
      option Option C
      option Option D

  .demo-examples(style = "max-width: 280px")
    select.ui-select(
      placeholder="Select option"
    )
      option Option A
      option Option B
      option Option C
      option Option D

  .demo-examples(style = "max-width: 280px")
    select.ui-select.ui-select--large(
      placeholder="Select option"
    )
      option Option A
      option Option B
      option Option C
      option Option D
