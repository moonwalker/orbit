$component-name = 'select';

@require '../_variables';
@import '../_selectors';

/*
 * Select
 */

+root() {
  appearance: none;
  border: 1px solid $muted-color;
  border-radius: 0;
  padding: scalc('%s - %s', $space-xsmall, 1px);
  bg($neutral-0);
  color: $muted-color;
  width: 100%;
  type-scale(medium);

  transition: $state-transition-out if $state-transition-out;
  outline: none;

  // Placheolder
  +placeholder() {
    color: rgba(@color, 0.6);
  }

  &:focus {
    border-color: $neutral-500;
    color: $body-color;
    transition: $state-transition-in if $state-transition-in;
  }
}

/*!
 * States
 */

/*!
 * Valid state
 */
+state('valid') {
  border-color: $success-color;
  transition: $state-transition-in if $state-transition-in;
}

/*!
 * Invalid state
 */
+state('invalid') {
  border-color: $danger-color;
  transition: $state-transition-in if $state-transition-in;
}

/*!
 * Size variations
 */
+modifier('small') {
  padding: scalc('%s - %s', $space-xxsmall, 1px);
  type-scale(small);
}

+modifier('large') {
  padding: scalc('%s - %s', $space-small, 1px);
  type-scale(large);
}
