$component-name = 'label';

@require '../_variables';
@import '../_selectors';

/*!
 * Label
 */
+root() {
  block();
}

/*!
 * Size variations
 */
+modifier('small') {
  type-scale(small);
}

+modifier('large') {
  type-scale(large);
}
