const assert = require('../../utils/assert');

describe('Position', () => {
  test('position', (done) => {
    assert(
      __dirname,
      'position',
      done,
    );
  });

  test('absolute', (done) => {
    assert(
      __dirname,
      'position-absolute',
      done,
    );
  });
});
