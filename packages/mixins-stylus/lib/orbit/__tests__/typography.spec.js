const assert = require('../../utils/assert');

describe('Typography', () => {
  test('type-scale', (done) => {
    assert(
      __dirname,
      'typography-type-scale',
      done,
    );
  });
});
